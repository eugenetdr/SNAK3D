module food (
    input clk,  // clock
    input rst,  // reset
    input wefood,
    output food_pos[12],
  ) {
  .clk(clk) {
    .rst(rst) {
      dff pos_x[4];
      dff pos_y[4];
      dff pos_z[4];
    }
  }  
  always {
    pos_x = 0;
    pos_y = 0;
    pos_z = 0;

    if(wefood) {
      //TODO: assign random values to pos_x, pos_y, pos_z
    }    

    food_pos = c{pos_x, pos_y, pos_z};
  }
}
